import template from './searchRepos.html';
import controller from './searchRepos.controller';

const searchReposComponent = {
	restrict: 'E',
	bindings: {},
	template,
	controller,
	controllerAs: 'vm'
};

export default searchReposComponent;
